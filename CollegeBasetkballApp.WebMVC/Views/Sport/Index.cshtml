@model IEnumerable<CollegeSportsApp.Models.SportModels.SportListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SportDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SportName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SportDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SportName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.SportId  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.SportId }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.SportId  })
        </td>
    </tr>
}

</table>
